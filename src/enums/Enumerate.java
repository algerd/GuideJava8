
package enums;

/**
 *
 * @author Alex
 */
public enum Enumerate {
    /*
    The body of the enum type can have a list of comma-separated constants and other elements that
    are similar to the elements you have in a class, for example, instance variables, methods, etc.
    */
    LOW, MEDIUM, HIGH, URGENT; // The semicolon after the last enum constant is optional if there is no code that follows the list of constants.

    // allow code
    public void get() {
        
    }
}
